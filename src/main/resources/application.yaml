server:
  port: 8081

spring:
  application:
    name: CustomerService
  datasource:
    url: jdbc:postgresql://localhost:5432/logisticsdb
    username: admin
    password: admin123
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: customer
    show-sql: true
  liquibase:
    default-schema: customer
    change-log: classpath:/db/changelog/customerChangelog.xml
